 enum Events {
    CONNECTION = 'connection',
    DISCONNECT = 'disconnect',
    CONNECTIONTOGAME = 'connectiontogame',
    GAMESTART = 'gamestart',
    MOVE = 'move',
    MOVED = 'moved',
    GAMEOVER = 'gameover'
};

export default Events;